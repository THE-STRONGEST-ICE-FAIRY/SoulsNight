package main.objects;

import java.awt.*;

public class Decoration extends Object {

    public Decoration(Image image, int x, int y, int w, int h) {
        super(image, x, y, w, h);
        id = 1;
    }
}
